# Ralph Progress Log
---

## 2026-03-18 — US-001: Define product metafield for max height ✅

**What was done:**
- Created metafield definition via Shopify Admin GraphQL API:
  - Namespace: `custom`, Key: `max_boy_cm`, Type: `number_integer`
  - Pinned: true (accessible in Liquid via `product.metafields.custom.max_boy_cm`)
  - Definition ID: `gid://shopify/MetafieldDefinition/408730304593`
- Updated BORNOVA Tül Perde (gid://shopify/Product/15726200684625):
  - Added tag `perde-tasarla`
  - Set metafield `custom.max_boy_cm = 270`
- Verified in browser (Playwright): navigated to https://inanctekstil.store/products/tul-bornova, confirmed product page loads and tags include `perde-tasarla`
- Auth method: client_credentials OAuth flow using credentials from .mcp.json

**Notes:**
- Shopify CLI does not store Admin API tokens on disk; used `shopify-mcp`'s client credentials flow to obtain `shpat_*` token for raw GraphQL mutations
- Playwright MCP required clearing `/Users/semsettin/Library/Caches/ms-playwright/mcp-chrome-770bf8f` because Chrome was already running

---

## 2026-03-18 — US-002: Curtain configurator section scaffold ✅

**What was done:**
- Created `technical/theme/sections/curtain-configurator.liquid`
  - Renders only when `product.tags contains 'perde-tasarla'`
  - CSS hides `variant-picker`, `.buy-buttons-block`, `sticky-add-to-cart` on tagged products
  - Empty `#curtain-configurator` container rendered with `data-max-boy` attribute
- Updated `technical/theme/templates/product.json`:
  - Added `curtain_configurator` section above `product_recommendations_qggXJq`
- Pushed to unpublished theme #193719566417 — push succeeded
- Verified via Playwright on preview URL:
  - BORNOVA Tül Perde (tagged): no variant-picker, no buy buttons, no sticky bar ✅
  - HÜRREM Fon Perde (untagged): full buy button UI visible ✅

**CSS selectors discovered:**
- `variant-picker` — custom element for variant selection
- `.buy-buttons-block` — class on buy buttons wrapper
- `sticky-add-to-cart` — custom element for sticky bar

---

## 2026-03-18 — US-003: Step 1 — Ölçü Seçimi ✅

**What was done:**
- Updated `curtain-configurator.liquid` with full Step 1 UI:
  - SVG curtain diagram with EN and BOY dimension arrows
  - En (cm) input: min 50, error "En en az 50 cm olmalıdır"
  - Boy (cm) input: max from `product.metafields.custom.max_boy_cm`, error "Boy en fazla X cm olabilir"
  - Devam Et button disabled until both inputs valid
  - JS stores values in `window.perdeConfig` on click
- Pushed to unpublished theme #193719566417
- Verified via Playwright: entering 30cm shows error + button disabled; 200x250 enables button ✅

---

## 2026-03-18 — US-004: Step 2 — Pile Stili visual picker ✅

**What was done:**
- Steps now use `is-active` CSS class to show/hide (hidden by default via `display:none`)
- Step 1 completion collapses to summary "Ölçü: X x Y cm", reveals Step 2
- Three pile cards with placeholder SVG illustrations: Düz Dikiş (x2.0), Kanun Pile (x2.5), Boru Pile (x3.0)
- Clicking a card: adds `is-selected` + `aria-pressed=true`, stores pile_stili and pile_orani in `window.perdeConfig`
- Keyboard accessible (Enter/Space triggers click)
- Devam Et disabled until a card is selected
- Verified via Playwright: all three cards visible, selecting Kanun Pile highlights it and enables Devam Et ✅

---

## 2026-03-18 — US-005: Step 3 — Kanat Seçimi ✅

**What was done:**
- Added CSS for `.perde-conf__kanat-options` grid and `.perde-conf__kanat-option` cards
- Added collapsed summary row `#perde-summary-3` (shows "Kanat: ...")
- Added Step 3 HTML with two options:
  - Tek Kanat (count=1): SVG of single panel, description "Tek parça perde, tek tarafa açılır."
  - Çift Kanat (count=2): SVG of two panels, description "İki parça perde, ortadan her iki yana açılır."
- Updated step2Btn click handler to reveal Step 3 (`step3El.classList.add('is-active')`)
- Added Step 3 JS: clicking an option sets `is-selected`/`aria-pressed`, stores `perdeConfig.kanat` and `perdeConfig.kanat_count`, enables Devam Et
- step3Btn click collapses Step 3 and shows summary (Step 4 to be added in US-006)
- Pushed to unpublished theme #193719566417 — success

**Verified via Playwright:**
- Step 1: entered 200x250, Devam Et enabled, clicked ✅
- Step 2: selected Kanun Pile, Devam Et enabled, clicked ✅
- Step 3 revealed with "3. Kanat Seçimi" heading and both options visible ✅
- Devam Et disabled until an option selected ✅
- Clicked Tek Kanat: card highlighted ([pressed]), Devam Et enabled ✅

---

## 2026-03-18 — US-007: Initialize Shopify App project ✅

**What was done:**
- Scaffolded app at `technical/design-your-curtain-shopify-app/`:
  - `shopify.app.toml` — configured for store 1z7hb1-2d.myshopify.com; `client_id` left as placeholder (requires Shopify Partners account to fill in)
  - `package.json` — bun project with `typecheck` and `lint` scripts targeting the extension
  - `.eslintrc.cjs` — TypeScript ESLint config
  - `bun.lock` — lockfile committed for reproducible installs
- Scaffolded Cart Transform extension at `extensions/cart-transform/`:
  - `shopify.extension.toml` — type=function, target=purchase.cart-transform.run
  - `tsconfig.json` — strict TypeScript, moduleResolution: bundler
  - `src/run.ts` — placeholder stub with hand-written CartTransformInput/Result types; TODO comment pointing to US-008 for implementation
- Ran `bun install` — 126 packages installed
- `bun run typecheck` — PASS (tsc --noEmit exits 0)
- `bun run lint` — PASS (eslint exits 0)
- `README.md` — documents Partners account prerequisite, setup steps, deploy command, project structure

**Notes:**
- `shopify app init` requires Shopify Partners login interactively; files created manually following Shopify CLI 3.x conventions
- `client_id` in shopify.app.toml must be filled after Partners account creation + app registration
- After first `shopify app deploy`, run `shopify app function typegen` to replace the hand-written type stubs with generated types from the Cart Transform schema

---

## 2026-03-18 — US-006: Step 4 — Onay, live price, and add to cart ✅

**What was done:**
- Added `data-base-price` and `data-variant-id` attributes to `#curtain-configurator` div (from Liquid)
- Added CSS for Step 4: review table, price block, disclaimer, cart error
- Added Step 4 HTML (`#perde-step-4`):
  - Review table: En, Boy, Pile Stili, Kanat
  - Price block: "Tahmini Fiyat" + formatted TL value + "Tahmini fiyat. Sepette kesinleşir."
  - Disclaimer checkbox (yellow tinted bg)
  - `#perde-cart-error` error block (hidden by default)
  - "Sepete Ekle" button (disabled until checkbox checked)
- Updated step3Btn click handler to call `showStep4()`
- Added Step 4 JS: `showStep4()` populates review + calculates price; checkbox toggles button; fetch POST to `/cart/add.js` with `_en`, `_boy`, `_pile_stili`, `_pile_orani`, `_kanat` properties; on success dispatches `cart:refresh` and calls `cart-drawer-component.open()`; on error shows Turkish error inline
- Fixed cart drawer selector: Horizon uses `cart-drawer-component` (not `cart-drawer`)
- Pushed to unpublished theme #193719566417 — success

**Verified via Playwright:**
- Full flow: 200x250 cm → Kanun Pile → Tek Kanat → Step 4 shows ✅
- Review table populated correctly ✅
- Price: 1.150,00 TL for 200 cm × 2.5 × 1 × 230 TL = 1150 ✅
- Price: 862,50 TL for 150 cm × 2.5 × 1 × 230 TL = 862.50 ✅
- Sepete Ekle disabled until disclaimer checked ✅
- After checking disclaimer and clicking Sepete Ekle: button shows "Sepete Eklendi ✓" ✅
- Horizon cart drawer (`cart-drawer-component`) opened with item in cart ✅

