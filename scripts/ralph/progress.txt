# Ralph Progress Log
Started: 20 Mar 2026 Cum +03 16:10:30
---
## US-001 — Backend project scaffold (20 Mar 2026)
Created technical/room-visualizer/ with:
- package.json (hono, pino, @fal-ai/client, @boundaryml/baml, biome, typescript)
- tsconfig.json (ESNext, strict, bundler resolution)
- Dockerfile (oven/bun:1.2-alpine, port 3000)
- src/index.ts (Hono, CORS allow inanctekstil.store, bodyLimit 12MB, GET /health)
- src/config.ts (all required env vars with defaults: SCORE_THRESHOLD=7, MAX_ATTEMPTS=3, FAL_TIMEOUT_MS=45000)
- src/lib/logger.ts (pino JSON logger)
Both `bun run typecheck` and `bun run lint` pass.

## US-008 — Frontend build setup (20 Mar 2026)
- Created technical/theme/package.json with esbuild build script (IIFE, ES2017)
- Created technical/theme/tsconfig.json (target ES2017, strict, noEmit)
- Created placeholder technical/theme/assets/room-visualizer.ts (exports empty initRoomVisualizer)
- `npm run build` produces valid IIFE room-visualizer.js (starts with `(() => {`)
- Added technical/theme/.gitignore to exclude node_modules

## US-007 — POST /api/visualize route (20 Mar 2026)
- Created src/routes/visualize.ts: handles POST /api/visualize (multipart/form-data)
- Validates product_id, room_image (type/size), rate limits 5/min per IP (in-memory)
- Returns 400/404/429/502 JSON errors with CORS headers; 200 image/jpeg with X-* headers
- Logs visualize_start and visualize_complete with duration_ms
- Registered route on app in src/index.ts
- tests/visualize.test.ts: 5 tests (CORS on 400/404/502, success, file type validation) — all pass
- typecheck and lint pass

## US-006 — fal.ai client with retry loop (20 Mar 2026)
- Created src/lib/fal.ts: generateRoomImage(roomImageBuffer, productData) with full retry loop
- Uploads room image to fal.ai storage, calls FAL_MODEL with room+curtain+prompt+seed
- Per-attempt timeout via AbortController; downloads result binary immediately
- Retries if score < SCORE_THRESHOLD OR has_curtains == false (up to MAX_ATTEMPTS=3)
- Returns highest-scored binary from all attempts
- Logs fal_call, fal_downloaded, eval_result events via pino
- tests/fal.test.ts: 3 tests (highest score wins, has_curtains=false retry, 3-attempt max) — all pass
- typecheck and lint pass

## US-005 — BAML evaluator (20 Mar 2026)
- Created baml_src/clients.baml: ClaudeOpus client (aws-bedrock, anthropic.claude-opus-4-6, region from env)
- Created baml_src/evaluator.baml: EvaluateRoomImage function → EvaluationResult{score, has_curtains, feedback}
- Generated baml_client/ with `node baml/cli.js generate`
- Created src/lib/evaluator.ts: evaluateImage(imageBuffer, productName) with setEvaluatorClient() for test injection
  (used injectable pattern to work around bun ESM static import validation of BAML's toBamlError)
- tests/evaluator.test.ts: 3 tests using setEvaluatorClient mock — all pass
- typecheck and lint pass

## US-004 — Prompt builder (20 Mar 2026)
- Created src/lib/prompt-builder.ts: buildPrompt(product, attempt, seedFn?) returns {prompt, seed}
- Attempt 1: base prompt with color+type; attempt 2+: appends prominence text
- seed injected via seedFn for deterministic testing
- tests/prompt-builder.test.ts: 4 tests (base prompt, retry prompt, seed type, deterministic seed) — all pass
- typecheck and lint pass

## US-003 — Shopify + PMS product data fetcher (20 Mar 2026)
- Created src/lib/shopify.ts: getProductData(productId) via Shopify Admin GraphQL, returns {title, type, color, imageUrl, sku}
- Created src/lib/pms.ts: getPmsSwatchUrl(sku) from PMS, returns null on failure (non-blocking, 5s timeout)
- PMS swatch preferred over Shopify image; falls back gracefully
- tests/shopify.test.ts: 3 tests (product found + PMS swatch, PMS fallback, product not found) — all pass
- typecheck and lint pass

## US-002 — DNS record + Docker compose entry (20 Mar 2026)
- Added `hcloud_zone_record "visualizer"` to technical/gitopsprod/dns.tf (A, 5.75.165.158)
- Created technical/room-visualizer/docker-compose.service.yml with Traefik labels for visualizer.inanctekstil.store, TLS, and 15MB body buffer
- Created technical/room-visualizer/README.md documenting all env vars and deploy commands
Both `bun run typecheck` and `bun run lint` pass.

## US-009 — Room visualizer modal, all 5 states (20 Mar 2026)
- Created technical/theme/assets/room-visualizer.ts: full modal implementation (~480 lines)
- initRoomVisualizer(config) exposed on globalThis (IIFE global scope)
- State 1 Upload: dropzone + file input + camera capture (accept=image/*, capture=environment on mobile)
- State 2 Curtain Picker: shown when productId absent; reads dataProductsJson; grouped by type
- State 3 Loading: dimmed uploaded photo + spinner + 'Yapay zeka odanızı hazırlıyor'
- State 4 Result: Blob URL image + X-Product-Title decoded title + Kaydet download + Sipariş Ver CTA
- State 5 Error: 'Görsel oluşturulamadı.' + Tekrar Dene button → back to State 1
- npm run build produces valid IIFE room-visualizer.js (15.7KB)
- Pushed to Shopify theme 193714913361 via shopify theme push
- Playwright verification: modal opens in State 1, initRoomVisualizer is function, overlay renders

## US-010 — Product page Odanda Gör button snippet (20 Mar 2026)
- Created technical/theme/snippets/room-visualizer-button.liquid
  - Renders "Odanda Gör" button only when product.tags contains 'odanda-gor'
  - Loads room-visualizer.js synchronously, attaches click listener to initRoomVisualizer
  - initRoomVisualizer called with productId (admin_graphql_api_id), productHandle, apiUrl
- Updated technical/theme/templates/product.json: added odanda_gor_button (custom-liquid) section
  - Placed between "main" and "curtain_configurator" in order array
  - custom_liquid: "{% render 'room-visualizer-button', product: product %}"
- Pushed snippets/room-visualizer-button.liquid + templates/product.json to theme 193714913361
- Added 'odanda-gor' tag to HÜRREM Fon Perde (fon-hurrem) via Shopify MCP for testing
- Playwright verification: button visible on /products/fon-hurrem, clicking opens modal in State 1 (picker skipped)

## US-011 — Dedicated /pages/odanda-gor landing page (20 Mar 2026)
- Created technical/theme/sections/odanda-gor-page.liquid with:
  - Hero: "Perdenizi Odanızda Görün" headline + subtitle
  - 3-step explainer: Fotoğraf Yükle / Perde Seç / Yapay Zeka Eklesin
  - "Hemen Dene" button calls initRoomVisualizer with dataProductsJson (no productId)
  - Liquid builds products JSON from collections (tul/fon/blackout/saten) via nested for loops
  - Products serialized as [{id, handle, title, imageUrl, type}] in data-products attribute
  - Section schema with presets defined
- Created technical/theme/templates/page.odanda-gor.json (custom page template)
- Created Shopify page "Odanda Gör" with handle "odanda-gor" (visible) via Admin
- Assigned page.odanda-gor template to the page
- Updated room-visualizer.ts: when no productId, modal opens in State 2 (picker) not State 1
  - After curtain selected → goes to State 1 (upload) if no photo yet
  - proceedFromUpload: if selectedProduct already set, skips picker and goes to loading
- Pushed sections/odanda-gor-page.liquid + templates/page.odanda-gor.json to theme 193714913361
- Playwright verification: /pages/odanda-gor renders hero + 3-step + Hemen Dene button,
  clicking opens modal in State 2 with 3 products (FON/BLK/STN) grouped by type
