interface RoomVisualizerConfig {
  productId?: string;
  productHandle?: string;
  apiUrl: string;
  dataProductsJson?: string;
}

export function initRoomVisualizer(_config: RoomVisualizerConfig): void {
  // Placeholder — full implementation in US-009
}
